{
  "id": "demo-stream",
  "version": "1.0.0",
  "name": "Live Demo Stream (addon)",
  "description": "Points an instance's sv_demoStream* cvars at the central demo-stream relay and registers its route with ql-stats-hub, so a spectator can watch a POV live. Unlike the telemetry relay there is nothing to install per host - the relay is one address for the whole cluster.",
  "scopes": ["global", "host", "instance"],
  "ui_api": 1,

  "ui": {
    "icon": "share",

    "instance_menu": [
      { "id": "demo-stream", "label": "Live Demo Stream (addon)", "icon": "share", "panel": "instance_stream" }
    ],
    "settings_section": {
      "label": "Demo Stream Relay (addon)", "icon": "share", "panel": "relay_settings"
    },

    "panels": {
      "instance_stream": {
        "kind": "form",
        "description": "Wires sv_demoStream* into this instance's server.cfg and registers its per-instance token with stats-hub. Requires the relay address set below. Applies config and restarts the instance.",
        "load": "GET instances/{instance_id}",
        "submit": "POST instances/{instance_id}/enable",
        "status": { "load": "GET instances/{instance_id}/status" },
        "fields": [
          { "key": "enabled", "type": "bool", "label": "Stream this instance", "description": "Saving with this on wires the cvars and restarts the instance." }
        ]
      },

      "relay_settings": {
        "kind": "form",
        "description": "The central relay's TCP ingest address. One address for the whole cluster - every streaming instance connects to it.",
        "load": "GET relay",
        "submit": "PUT relay",
        "fields": [
          { "key": "host", "type": "string", "label": "Relay host", "placeholder": "stats.example or 10.0.0.5", "description": "Hostname or IP the game servers can reach." },
          { "key": "port", "type": "string", "label": "Relay port", "placeholder": "7788", "description": "TCP ingest port (STATS_HUB_DEMO_STREAM_TCP_PORT on the stats-hub side)." }
        ]
      }
    }
  }
}
